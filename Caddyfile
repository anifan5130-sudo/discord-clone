# HTTPS-сертификат Let's Encrypt выпускается автоматически.
{$DOMAIN} {
	# сигнальный канал LiveKit (WebSocket)
	handle /rtc* {
		reverse_proxy 127.0.0.1:7880
	}
	# сайт, API и поток событий
	handle {
		reverse_proxy 127.0.0.1:3000
	}
}
